# Note that you don't need to actually put files here but can rather just
# symbolic link to them for inclusion, just don't forget to build them before
# you use them
add_subdirectory(directory_in_this_directory_name)
